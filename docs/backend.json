
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Information about a user, including their role.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's full name."
        },
        "role": {
          "type": "string",
          "enum": ["admin", "customer"],
          "description": "The user's role in the system."
        }
      },
      "required": ["role"]
    },
    "Product": {
      "title": "Product",
      "description": "A luxury gift item available in the shop.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The name of the product as defined by the admin."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the product."
        },
        "price": {
          "type": "number",
          "description": "Price in MAD."
        },
        "shippingPrice": {
          "type": "number",
          "description": "Shipping cost for this product in MAD."
        },
        "categoryId": {
          "type": "string",
          "description": "Category ID the product belongs to."
        },
        "images": {
          "type": "array",
          "items": { "type": "string" },
          "description": "URLs of product images."
        },
        "featured": {
          "type": "boolean"
        },
        "bestSeller": {
          "type": "boolean"
        }
      },
      "required": ["title", "price", "shippingPrice", "categoryId"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Collection of user profiles containing roles and metadata."
    },
    "/products/{productId}": {
      "schema": "Product",
      "description": "Collection of products managed by the admin."
    }
  }
}
